# No Spec Changes

This change is a pure implementation fix — replacing JDK exceptions with `RinkoException` subclasses. API contracts, HTTP status codes, and error formats remain the same (or are corrected to proper semantics). No behavioral requirements change.
